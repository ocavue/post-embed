---
'@post-embed/elements': patch
---

Replace `lit-html` with a typed native DOM factory and native event listeners. Snapshot updates rebuild the card, while media reveals and copy feedback update their existing nodes.
