---
"@post-embed/exporter": minor
"@post-embed/schema": patch
"@post-embed/types": minor
---

Accept quotes without thread metadata and return `{ value }` or `{ issues }` from `fromSyndication` instead of silently returning `undefined` on invalid data.
