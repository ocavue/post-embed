---
'@post-embed/elements': minor
---

Restyle X cards with 12px corners, a subtle border, and 28px avatars. Render X cards and their built-in theme in an open shadow root to isolate them from page CSS. Existing theme variables remain supported; descendant CSS selectors and DOM queries must account for the shadow boundary.
